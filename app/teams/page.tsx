const AdminPageNotProtected: React.FC = () => {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between p-24">
      Welcome to the teams page that is NOT protected
    </main>
  );
};

export default AdminPageNotProtected;
